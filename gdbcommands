target extended-remote | openocd -l openocd.log -f interface/cmsis-dap.cfg -f target/rp2040.cfg -c "gdb_port pipe; log_output openocd.log; adapter speed 5000"

define bl
  make -C build
  if $_shell_exitcode == 0
    file build/src/pio_display.elf
    load
    monitor reset init
  end
end

define blc
  bl
  continue
end
